{
    "name": "analyze-transcript-app",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "dev": "echo \"No dev script configured, serving static files or relying on Vercel's default for TSX.\"",
      "build": "echo \"No build script configured, relying on Vercel's handling of TSX and import maps.\"",
      "start": "echo \"No start script configured.\""
    }
  }